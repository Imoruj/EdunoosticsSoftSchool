# Docker Compose for Edunostics Development Database

services:
  postgres:
    image: postgres:16-alpine
    container_name: edunostics_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: edunostics
      POSTGRES_PASSWORD: edunostics_dev_2024
      POSTGRES_DB: edunostics_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U edunostics -d edunostics_db"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Optional: Adminer for database management UI
  adminer:
    image: adminer:latest
    container_name: edunostics_adminer
    restart: unless-stopped
    ports:
      - "8080:8080"
    depends_on:
      - postgres

volumes:
  postgres_data:
